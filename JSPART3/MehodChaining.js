// Methdod Chaining

// Using one method after another. order of execution wiil be left to right.

let msg = "  hellow   ";
// let newMsg = msg.trim();
// console.log(newMsg);

// newMsg = newMsg.toUpperCase();
// console.log(newMsg);

let newMsg = msg.trim().toUpperCase();
console.log(newMsg);