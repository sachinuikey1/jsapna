// returns true if  every element of arryay gives true for some function. Else returns false.

// arr.every(some function definition or name);

console.log([2,4,6].every((el) => el % 2 == 0));

console.log([2,4,6, 9].every((el) => el % 2 == 0));


